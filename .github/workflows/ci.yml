name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r backend/requirements.txt
    - name: Run tests
      run: |
        cd backend
        # Set dummy env vars for testing
        export GOOGLE_API_KEY=dummy
        export LLM_PROVIDER=mock
        export FIREBASE_CREDENTIALS=dummy
        pytest tests/

  frontend-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    - name: Install dependencies
      run: |
        cd frontend
        npm install
    # Skipping build/test for now as we don't have a full test suite or build config set up for CI yet
    # - name: Build
    #   run: |
    #     cd frontend
    #     npm run build
